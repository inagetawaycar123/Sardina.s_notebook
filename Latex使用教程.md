---
date: 2025.11.19
tags:
  - 数学
  - 小配件
---
在obsidian中，只要使用`$[Latex代码]$`将代码括入其中，就能实现渲染功能

#### 上标
$a^2 + b^2 = c^2$
比如，上面陈列的公式只需要`a^2 + b^2 = c^2`就能完美表示

$x^{1/2}$ = `x^{1/2}`
$x^\frac{1}{2}$ = `x^\frac{1}{2}`
$f'(x)$ = `f'(x)`


#### 下标
$a_1, a_n$ 写作`a_1, a_n`

#### 省略号
$\dots$  是`\dots`
$\cdots$  是`\cdots`，这个省略号长在行中间哟

#### 分式和根式
$\frac{3}{8}$   = `\frac{3}{8}`

$\dfrac{3}{8}$ = `\dfrac{3}{8}`

$\sqrt[y]{x}$  = `\sqrt\[y]{x}`

#### 运算符
$\ne \qquad \ge \qquad \le \qquad \approx \qquad \equiv \qquad \div$  
`\ne  \ge  \le  \approx  \equiv  \div`

$\sin \qquad \cos \qquad \arcsin \qquad \arctan$ 
`\sin  \cos  \arcsin  \arctan`
不一一列举，但是挺简单

$\Delta$ =  `\Delta`
$\lim_{x \to 0}$  = `\lim_{x \to 0}`
$\lim\limits_{x \to 0}$  =`\lim\limits_{x \to 0}`
$\lim_{x \to 0, y \to 0}$ = `\lim_{x \to 0, y \to 0}`
$\lim_{\substack{x \to 0 \\ y \to 0}}$  = `\lim_{\substack{x \to 0 \\ y \to 0}}`


#### 巨算符
$\sum_{i=1}^n$ = `\sum_{i=1}^n`

$\int_0^{\frac{\pi}{2}}$  =  `\int_0^{\frac{\pi}{2}}`














